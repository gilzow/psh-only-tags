name: fire when tagged
on:
  push:
    tags:
      - '*'

jobs:
  we-b-tagged:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: echo "The tag that triggered this workflow is ${{ github.ref_name }}"

